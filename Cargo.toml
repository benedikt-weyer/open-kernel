[workspace]
members = [
    "multiboot2-adapter-bootstrap",
    "kernel-core",
    "multiboot2-adapter",
    "limine-adapter",
    "user-program",
    "std-program",
]
resolver = "3"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
